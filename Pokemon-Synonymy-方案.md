# Pokémon Synonymy 游戏方案

## 1. 项目概述

这是一个受 Synonymy 启发的非商业宝可梦网页游戏。

游戏把每一种宝可梦视为一个节点，根据名称、图鉴介绍和属性资料计算宝可梦之间的相似度，再把相似的宝可梦连接成一张关系图。玩家从指定的起点出发，每一步选择一只与当前宝可梦相似的宝可梦，最终抵达目标。

示意：

```text
小火龙 → 火恐龙 → 快龙 → 拉普拉斯 → 杰尼龟
```

项目定位：

- 面向少量朋友游玩的中文网页游戏
- 免费、非商业、非官方同人作品
- 第一版覆盖当前 PokéAPI 可取得的完整全国图鉴；关都 151 只仅作为开发期快速验证集
- 数据预先生成，游戏过程中不实时调用 AI
- 每局由版本化随机种子确定唯一的随机起点与随机目标，分享挑战码即可复现同一局

## 2. 核心体验

玩家进入一局后看到：

- 起始宝可梦
- 目标宝可梦
- 当前宝可梦的图片、名称和简短介绍
- 4～8 个可选择的相似宝可梦
- 已走过的路径和步数

选择下一只宝可梦后，系统更新候选项，直至抵达目标。

建议提供以下辅助反馈：

- `变近 / 变远`：新节点到目标的最短距离是否减少
- `相似原因`：例如同属性、同蛋群、图鉴都提到火焰
- `撤回`：每局允许有限次数回到上一步
- `提示`：指出一个更接近目标的选项，但不直接展示完整答案

完成后展示：

- 玩家路径
- 实际步数
- 理论最短步数
- 用时
- 是否走过重复节点
- 可复制的分享结果

挑战链接在开局预览、游玩途中和通关后都可以复制。未通关时分享当前已走步数；通关后分享最终步数。链接只复现挑战题目与模式，不携带分享者当前路径。

## 3. MVP 范围

第一版使用当前 PokéAPI 可取得的完整全国图鉴物种集合。游戏核心体验来自大量跨世代数据形成的语义联系，因此 151 只仅作为开发期快速验证集，不作为正式发布范围。

### 必做功能

1. 根据随机种子，从完整图鉴中等概率抽取一个有序的“起点—目标”组合。
2. 起点不得等于目标，且起点到目标的有向最短路径至少为 3 步；不满足时沿同一伪随机序列继续抽取。
3. 每一步展示 6 个左右的相似邻居。
4. 展示当前路径、步数、目标和挑战码。
5. 抵达目标后显示结算页。
6. 支持重新开始、输入挑战码和生成新挑战。
7. 同一数据版本、图版本、算法版本和随机种子必须生成完全相同的挑战。
8. 所有游戏数据以本地静态 JSON 提供。
9. 支持手机和桌面浏览器。

### 第二阶段功能

- 每日固定题目
- 简单模式（显示完整关系地图）与困难模式（不显示地图）
- 提示与撤回次数限制
- 分享结果
- 最短路径回放
- 图鉴收集或已发现节点记录
- 扩展到后续世代

### 暂不实现

- 用户注册和排行榜
- 实时多人对战
- 实时调用大语言模型判断相似度
- 玩家自由输入任意宝可梦名称
- 全部特殊形态、超级进化和地区形态

## 4. 数据来源

使用 PokéAPI v2：

```text
https://pokeapi.co/api/v2/pokemon/{id}
https://pokeapi.co/api/v2/pokemon-species/{id}
```

`pokemon` 主要获取：

- 属性 `types`
- 特性 `abilities`
- 身高、体重
- 种族值
- 图片 `sprites`

`pokemon-species` 主要获取：

- 多语言名称 `names`
- 图鉴介绍 `flavor_text_entries`
- 颜色 `color`
- 外形 `shape`
- 栖息地 `habitat`
- 蛋群 `egg_groups`
- 世代 `generation`
- 进化链 `evolution_chain`
- 传说、幻之宝可梦标记

优先图片字段：

```text
sprites.other.official-artwork.front_default
```

如果图片缺失，回退到：

```text
sprites.front_default
```

## 5. 数据获取与缓存

不要在浏览器打开游戏时逐只请求 PokéAPI。应编写一个离线构建脚本，完成以下工作：

```text
PokéAPI
  → 下载原始数据
  → 保存到本地缓存
  → 清洗与归一化
  → 生成相似度向量
  → 建立关系图
  → 验证连通性和题目
  → 输出前端静态 JSON
```

建议目录：

```text
data/
  raw/                 # PokéAPI 原始响应，不提交也可以
  processed/           # 清洗后的标准数据
  game/                # 前端直接加载的数据
scripts/
  fetch-pokemon.*      # 下载与缓存
  build-documents.*    # 生成用于向量化的文本
  build-graph.*        # 计算邻居并构建图
  validate-graph.*     # 检查孤立点、路径和题目质量
src/                   # 游戏前端
```

每次构建时优先读取缓存，只请求缺失记录，避免重复访问公共 API。

## 6. 数据清洗规则

### 名称

- 优先使用简体中文名称。
- 保留英文和日文名称作为辅助文本，但不必直接展示。
- 对缺少中文名称的数据提供英文回退。
- 第一版按“物种”而非“形态”建节点，避免一个物种出现大量特殊形态。

### 图鉴介绍

同一宝可梦可能在多个游戏版本中拥有大量重复或近似描述。建议：

1. 替换换行、换页符和异常空格。
2. 按语言筛选文本。
3. 对完全相同的句子去重。
4. 对高度近似的句子只保留一条。
5. 最多保留 3～5 条信息差异明显的介绍。
6. 中文介绍不足时，可以使用英文介绍做向量计算，但界面应尽量展示中文。

不要把所有版本的重复介绍直接拼接，否则重复内容会在向量中获得不合理的高权重。

### 分类、体型、脚印与图鉴颜色

- 分类取自 `pokemon-species.genera`，优先简体中文，缺失时回退英文。当前中文覆盖 1010/1025，英文覆盖 1025/1025。分类会同时进入语义文档和结构化相似度，属于较强特征。
- 体型取自 `pokemon-species.shape`，当前覆盖 1025/1025，作为低权重结构化特征。
- 图鉴颜色取自 `pokemon-species.color`，当前覆盖 1025/1025，作为低权重结构化特征。
- PokeAPI v2 和其官方 sprites 仓库目前都没有脚印字段。标准化结构暂时保留 `footprint: null`，权重为 0；只有在找到覆盖完整图鉴且许可清楚的稳定来源后才启用，缺失值不得被当作“相同脚印”。

## 7. 相似度方案

### 7.1 双语义向量

不再把结构化资料和全部图鉴描述拼成一个长文本，而是为每只宝可梦生成两组彼此独立的向量。

第一组是结构化档案向量。字段顺序和措辞使用固定模板，避免语序变化造成无意义波动：

```text
passage: 宝可梦结构化档案。
分类：火焰宝可梦。
形态：双翼双足。
栖息地：山地。
蛋群：怪兽、龙。
特性：猛火、太阳之力。
图鉴颜色：红色。
```

结构化档案不包含名称、图鉴描述和属性。名称和属性都有独立分数，不重复放入档案向量，以免实际影响显著高于表面权重。

第二组是图鉴描述向量。处理步骤如下：

1. 使用 Unicode NFKC 统一字符形式，移除换行、换页符和异常空格。
2. 删除完全相同的重复描述；第一版不做语义近似去重，避免误删含义不同的事实。
3. 每条去重后的图鉴描述独立添加 `passage:` 前缀并生成归一化向量，不把全部版本拼成超长文本。
4. 对同一宝可梦的所有描述向量求平均，再次进行 L2 归一化，得到该宝可梦的描述语义中心。
5. 分别缓存档案和描述的文档哈希、模型版本、向量维度及描述覆盖情况。

这种做法能避免长文本截断，也降低某个版本的语序、措辞或重复次数对最终近邻的影响。

第三个独立信号是中文名称字符相似度。名称不进入 Embedding，而是仅保留汉字、按每个字在全图鉴名称中的出现频率计算 IDF，再对名称字符向量计算余弦相似度。共享“鹿”“鲸”等相对少见的字会获得较明显加分；共享“小”“大”等常见字只产生弱加分。该信号是辅助关系，不应覆盖图鉴描述、分类或进化关系。

### 7.2 混合分数

推荐初始公式：

```text
总相似度 =
  0.38 × 图鉴描述向量相似度
+ 0.13 × 结构化档案向量相似度
+ 0.05 × 中文名称字符相似度
+ 0.12 × 分类相似度
+ 0.07 × 属性相似度
+ 0.07 × 蛋群相似度
+ 0.03 × 栖息地相似度
+ 0.025 × 体型相似度
+ 0.015 × 图鉴颜色相似度
+ 0.04 × 特性相似度
+ 0.04 × 进化链相似度
+ 0.03 × 种族值相似度
+ 0.00 × 脚印相似度（数据源确认后启用）
```

图鉴描述、结构化档案和中文名称三项合计仍为 0.56，结构化精确字段合计为 0.44，总权重为 1.00。若任意一方缺少有效图鉴描述，则该配对不使用描述向量，并把描述与档案合计的 0.51 权重分配给结构化档案向量；中文名称仍独立保留 0.05，避免缺失数据使宝可梦天然远离所有节点。

这些权重必须通过实际路径抽检调整，不应视为最终参数。调参时除个别相似度外，还应加入“同一描述不同改写的前 20 近邻重合率”，用来衡量表达方式变化是否会大幅改写关系。

结构化字段可使用 Jaccard 相似度：

```text
J(A, B) = |A ∩ B| / |A ∪ B|
```

连续数值如身高、体重和种族值可以先归一化，再计算距离。这类字段只适合低权重辅助，避免体型接近但毫无关联的宝可梦被大量连接。

### 7.3 进化链处理

进化链关系非常强，但如果不限制，妙蛙种子周围可能几乎都是自己的进化家族。

建议：

- 同一进化链给予少量加分，确保关系通常存在。
- 进化链只通过权重加分，不再硬性占用或限制候选位置；最终严格按总相似度取前 20 名。
- 起点与终点不因模式改变；两种模式使用相同的随机抽取和距离规则。

## 8. 关系图构建

使用固定 K 的有向近邻图，不使用单一相似度阈值。每只宝可梦严格按总相似度从高到低保留最近的 20 只，不再把双方邻居合并成无向边，因此所有节点的可选数量稳定为 20；前端每页展示 5 只，共 4 页。

若 A 的前 20 名包含 B，只表示玩家可以从 A 走到 B，不要求 B 的前 20 名也包含 A。建图后必须验证整张有向图强连通；若不强连通，不通过添加第 21 个候选修补，而应调整相似度或 K 值并重新生成。挑战生成器计算“某节点到目标”的距离时，需要从目标沿入边反向广度优先搜索，得到与玩家实际前进方向一致的最短距离。

### 防止枢纽节点垄断

某些兼具多属性、描述宽泛或关系丰富的宝可梦可能成为大量路径的必经点。需要统计每个节点的：

- 度数
- 被选为邻居的次数
- 最短路径经过次数
- 在题库最短路径中的出现频率

对过强枢纽减少弱边，或限制同一宝可梦在题库中的出现频率。

## 9. 随机种子、难度与出题

每局由随机种子稳定生成唯一的起点与目标。起点和目标都从完整图鉴均匀抽取，不再建立“合法起点池”，也不让玩家从开局候选中选择。

抽取采用拒绝采样：先独立抽取目标与起点；若两者相同，或起点到目标的有向最短路径少于 3 步，则继续消耗同一 PRNG 序列重新抽取。这样不会偏向“合法起点较多”的目标，并且所有通过约束的有序起点—目标组合保持等概率。

### 挑战码与可复现性

不要直接使用 Math.random() 出题。项目应固定一种跨浏览器结果一致的伪随机算法，例如项目内实现的 mulberry32 或 xoshiro。

同一个挑战必须同时固定：

- 数据集版本
- 关系图版本
- 出题算法版本
- 游戏模式
- 随机种子

当前分享码可采用 P1-G7-A2-E-002N9C。其中 P1 是图鉴数据版本，G7 是关系图版本，A2 是“随机起点与目标、最短距离至少 3 步”的出题算法版本，E 表示简单模式、H 表示困难模式，最后一段是 32 位随机种子的 Base36 表示。挑战码只接受 E 或 H，不再接受旧的 N 标记。G7 使用混合相似度，让每只宝可梦严格保留有向前 25 名，并在显式蛋群相似度和结构化 Embedding 文本中排除 `no-eggs`；分享链接可使用 challenge 查询参数。

版本信息不可省略。否则未来更新图鉴、权重或算法后，同一种子会生成不同挑战。

### 确定性生成与验证

种子用于可复现地选择挑战。生成器执行确定性的“抽取—验证—重试”：

1. 用种子初始化 PRNG。
2. 从完整图鉴均匀抽取目标宝可梦。
3. 从完整图鉴独立、均匀抽取起点宝可梦。
4. 检查起点不等于目标，且有向最短路径至少为 3 步。
5. 不合格时继续消耗同一 PRNG 序列并重新生成。
6. 超过最大尝试次数时使用确定性的后备挑战。

同一版本和种子必须始终产生同一个最终结果。

不再预先建立目标池或开局候选池。当前 1025 个节点在图中全部可达，因此生成器只需检查“不同节点”和“最短路径至少 3 步”。以当前图为例，约九成随机有序组合可以直接通过，通常一次抽取即可完成。

### 模式与距离

对构建完成的图使用反向广度优先搜索，计算所抽起点到目标的有向最短路径。

当前 A2 版本提供两种模式：

- 简单模式（E）：显示完整关系地图，地图标记目标、当前节点与已走路径。
- 困难模式（H）：完全不创建或显示关系地图，只通过候选宝可梦及其描述判断方向。

模式不改变起点、目标、邻居或最短路径规则。同一种子分别使用 E 与 H 时会得到相同题目，只是可见信息不同。所有挑战统一要求理论最短路径至少 3 步。

题目应满足：

- 起点与目标不是同一只宝可梦。
- 起点到目标可达，且理论最短路径至少为 3 步。
- 除上述约束外不按属性、世代、进化链或节点连接数干预抽样。

每次抽取后必须验证距离；不合格组合使用同一随机序列继续重试，以同时保证可玩性与可复现性。

## 10. 相似原因解释

文本向量本身很难直接解释。界面中的“相似原因”应主要来自结构化字段和关键词，而不是让模型实时编造。

示例：

```json
{
  "from": 6,
  "to": 149,
  "score": 0.78,
  "reasons": [
    "都具有飞行属性",
    "都属于龙蛋群",
    "图鉴介绍都提到飞行"
  ]
}
```

如果两只宝可梦只有语义向量接近、没有可靠的结构化原因，可使用中性文案：

```text
它们的图鉴描述具有相似的生态或行为特征。
```

## 11. 前端数据格式

建议输出两个主要文件。

`pokemon.json`：

```json
{
  "6": {
    "id": 6,
    "name": "喷火龙",
    "nameEn": "Charizard",
    "image": "...",
    "description": "会在空中飞翔，寻找强大的对手……",
    "types": ["火", "飞行"]
  }
}
```

`graph.json`：

```json
{
  "graphVersion": 6,
  "layout": {
    "version": 1,
    "method": "umap-precomputed",
    "positions": {
      "6": [0.3124, 0.7815]
    }
  },
  "neighbors": {
    "6": [
      {
        "id": 149,
        "score": 0.78,
        "reasons": ["都具有飞行属性", "都属于龙蛋群"]
      }
    ]
  }
}
```

“layout.positions”是对完整综合相似度矩阵执行 UMAP 后得到的固定二维坐标。坐标在数据构建阶段一次性计算并归一化到 0—1；前端无需加载 Embedding 或运行降维算法，只按坐标绘制 SVG。

题库可以单独保存：

```json
[
  {
    "start": 4,
    "target": 7,
    "mode": "easy",
    "shortestDistance": 6
  }
]
```

前端只负责游戏状态、交互和展示，不参与相似度计算。

## 12. 页面结构

### 首页

- 游戏名称和一句话规则
- 开始随机游戏
- 每日挑战（第二阶段）
- 玩法说明
- 非官方同人声明

### 游戏页

- 顶部：当前宝可梦、目标宝可梦和步数
- 简单模式在桌面端按约 1:4 分栏：左侧为二维关系图，右侧为每页 5 只、共 4 页的相似候选
- 困难模式不创建地图，候选区域使用完整主工作区
- 所有主要交互严格控制在一个视口内，不依赖纵向滚动
- 简单模式的地图从游戏开始就显示全部 1025 个节点和 20500 条连接，不展示宝可梦图片或名称
- UMAP 坐标作为语义锚点，再离线式预热力导向模拟；节点排斥和碰撞使分布更均匀，少量高置信关系作为弹簧维持局部结构
- 地图稳定后围绕锚点进行约 1—1.6 像素的复合轻颤，线段随端点同步变化
- 玩家每次抵达新节点时，以新节点为中心发出约 1.8 秒的径向弹性波：邻近节点和连线依次外推并衰减回位
- 系统开启“减少动态效果”时停用轻颤和径向波动
- 目标为红点、当前选择为蓝点、已经走过的节点及路径为浅蓝、其他节点为灰色
- 辅助操作：提示、撤回、重新开始

### 结算页

- 成功状态
- 保留左侧完整关系图，并显示最终的整条浅蓝路径
- 目标点同时是当前点时，保留红色填充并增加蓝色外圈
- 实际步数与最短步数
- 用时和评级
- 查看一种最短路径
- 再来一局
- 复制分享文本

## 13. 技术建议

前端使用普通静态 Web 应用即可。适合的组合包括：

- React + TypeScript + Vite
- 静态 JSON 数据
- 浏览器 `localStorage` 保存设置和每日挑战状态
- 部署到 GitHub Pages、Cloudflare Pages 或类似静态托管服务

数据脚本可使用 Python或 TypeScript。若要使用本地文本向量模型，Python 的相关工具更成熟；若使用远程 Embedding API，两种语言都可以。

第一版不需要数据库和服务端。

## 14. 验证标准

在开始制作完整界面前，先生成一个关系图报告，至少检查：

- 是否有孤立节点
- 整张图是否连通
- 每个节点的邻居数量分布
- 最高度数节点
- 最常出现在最短路径中的节点
- 随机抽取 50 组邻居进行人工合理性评分
- 随机生成 30 道题并检查路径趣味性

推荐人工评分：

| 分数 | 含义 |
|---:|---|
| 0 | 完全无法理解为什么相似 |
| 1 | 勉强有关联，但非常牵强 |
| 2 | 关系可以理解 |
| 3 | 关系明显且有趣 |

目标是抽检平均分不低于 2，并消除所有 0 分强边。

## 15. 发布与素材注意事项

本项目只作为免费、非商业、少量朋友游玩的同人项目，但仍建议：

- 明确标注“非官方同人作品”。
- 说明项目与 Nintendo、Game Freak、The Pokémon Company 无关联。
- 不收费、不接广告、不接受与游戏访问挂钩的赞助。
- 不使用“官方”“正版授权”等容易造成混淆的表述。
- 标注数据来自 PokéAPI，并链接其网站。
- 不将 PokéAPI 的开放访问误认为宝可梦素材的商业授权。
- 如果权利方提出移除要求，应及时下线相关内容。

页面底部可以使用类似声明：

```text
本项目是免费、非商业的非官方宝可梦同人游戏，仅供学习和朋友间娱乐。
Pokémon 及相关角色、名称和图像的权利归其各自权利方所有。
本项目与 Nintendo、Game Freak、The Pokémon Company 无关联，也未获得其赞助或认可。
游戏数据来源于 PokéAPI。
```

这类声明不能代替正式授权，但可以减少用户误认为官方产品的可能性。

## 16. 推荐实施顺序

1. 用关都 151 只跑通数据抓取、清洗和图构建流程。
2. 下载并清洗当前完整全国图鉴的物种数据。
3. 生成每只宝可梦的标准语义文档。
4. 选择一种 Embedding 方案并计算文本向量。
5. 合并文本和结构化相似度。
6. 构建完整的带约束 KNN 图。
7. 输出邻居列表和图结构统计报告。
8. 人工抽检跨世代关系并调整权重。
9. 建立距离索引，实现固定 PRNG 和挑战码解析。
10. 使用固定种子集验证可复现性和题目质量。
11. 制作最小可玩的游戏页面。
12. 加入分享、每日挑战和视觉完善。

## 17. 成功标准

MVP 可以按以下标准验收：

- 当前纳入的全国图鉴物种均有名称、图片和可展示介绍，缺失项有明确回退策略。
- 没有孤立节点，所有题目都可达。
- 每只宝可梦有严格 20 个按混合相似度排序的有向邻居。
- 随机邻居人工抽检平均分不低于 2/3。
- 普通题平均可在 3～5 分钟内完成。
- 同一版本下，相同挑战码在不同浏览器生成完全相同的起点与目标。
- 任意正式分享的挑战码都能生成经过验证且可完成的题目。
- 玩家不需要了解相似度算法也能理解为什么可以从 A 走到 B。
- 手机浏览器可以完整完成一局。

## 18. 核心决策总结

- 第一版覆盖完整全国图鉴；关都 151 只仅作为开发验证集。
- 以物种为节点，暂不拆分特殊形态。
- 使用“文本向量 + 结构化资料”的混合相似度。
- 使用带约束的 KNN 图，不直接按阈值连边。
- 离线计算全部数据，前端只读取静态 JSON。
- 使用版本化随机种子生成唯一的随机起点与目标，并以确定性拒绝采样验证挑战。
- 起点不等于目标，且起点到目标的理论最短路径至少为 3 步。
- 相似原因由结构化数据生成，不实时依赖大语言模型。
- 免费、非商业发布，并清晰标注非官方同人属性。

## 19. 开发流程

项目按照“数据先行、图质量优先、界面后置”的顺序实施。每个阶段必须产生可检查的文件、报告或测试结果，再进入下一阶段。

### 阶段一：项目初始化

- 初始化 Git 仓库、React + TypeScript + Vite 前端和独立 Python 环境。
- 锁定 npm 与 Python 依赖版本。
- 建立 data、scripts、src、tests 和 reports 目录。
- 配置前端单元测试、浏览器测试和生产构建。
- 提供标准多文件构建与 Gist 单文件构建两种产物。

验收：空白应用可运行，测试和构建命令通过，Python 数据脚本可执行。

### 阶段二：完整图鉴数据管线

- 从 PokéAPI 动态读取当前物种总量，不写死图鉴数量。
- 获取 pokemon、pokemon-species 和进化链数据。
- 缓存原始响应，只下载缺失或需要更新的数据。
- 清洗为统一物种格式，并生成缺失字段报告。
- 第一版以物种为节点，特殊形态暂不单独建节点。

验收：每个纳入游戏的物种都有稳定 ID、名称、图片与介绍回退策略。

### 阶段三：语义文档与 Embedding

- 生成字段顺序一致且不含名称、属性、图鉴描述的结构化档案文档。
- 对图鉴描述做 NFKC 和空白规范化，并删除完全重复的版本文本。
- 每条图鉴描述单独生成归一化向量，同一宝可梦按向量均值聚合并再次归一化。
- 使用本地 multilingual-e5-small 模型分别生成档案向量和描述向量。
- 分别缓存两类向量、模型版本、文档哈希、描述覆盖掩码和聚合方式。

验收：所有物种均有结构化档案向量；有图鉴文本的物种均有描述向量；相同数据无需重复计算。

### 阶段四：相似度与关系图

- 分别计算图鉴描述语义、结构化档案语义、属性、蛋群、栖息地、外形、特性和进化关系。
- 以汉字 IDF 余弦相似度计算独立中文名称信号，降低常见字造成的误连接。
- 任一方缺少描述向量时，将两项语义权重合并回退给结构化档案向量。
- 通过配置文件混合各项权重。
- 先取得较大的近邻候选池，再严格保留每个节点得分最高的 20 个邻居。
- 保留有向 KNN 关系，验证整张图强连通并检查过强枢纽。
- 生成反向邻接索引，供挑战生成器计算有向最短路径。

验收：整图连通，节点度数合理，没有无记录的孤立节点。

### 阶段五：图质量报告与调参

- 生成每只宝可梦的邻居、相似度组成和解释原因。
- 统计低置信度边、桥梁边、枢纽节点和最短路径出现频率。
- 随机生成跨世代路径样本。
- 优先人工检查异常关系，再调整权重并重新建图。

验收：随机关系抽检平均分不低于 2/3，所有 0 分强边被移除或修正。

### 阶段六：随机种子挑战引擎

- 实现跨浏览器一致的固定 PRNG。
- 挑战码携带数据集、关系图、算法、游戏模式和种子版本。
- 由种子确定唯一的随机起点与目标。
- 使用确定性的抽取、距离验证与重试流程。
- 建立固定挑战码黄金测试样本，并批量验证随机种子。

验收：同一挑战码在不同运行环境产生完全相同且可完成的挑战。

### 阶段七：游戏前端

- 依次完成首页、挑战码输入、起点与目标预览、主游戏、路径记录、提示与撤回、结算和分享。
- 构建阶段用 UMAP 把完整相似度矩阵投影成稳定二维坐标；前端使用 Canvas 常驻绘制完整网络，并以受锚点约束的力导向布局均衡节点分布，根据游戏状态更新节点颜色。
- 采用手机优先的响应式布局，并适配桌面浏览器。
- 第一轮完成完整功能流程，第二轮再进行视觉、动效、可访问性和异常状态打磨。
- 视觉方向采用具有图鉴研究手册质感的自然探索风格，避免仿制宝可梦官方界面。

验收：用户无需理解图算法，也能从页面信息理解目标、候选和路径。

### 阶段八：自动化测试

- Python 测试覆盖数据清洗、文本生成、相似度、建图和连通性。
- 前端测试覆盖挑战码、游戏状态、胜利判定、撤回和分享。
- 浏览器测试覆盖完整一局、直接挑战链接、刷新恢复和移动端尺寸。
- 验证图片失败、数据加载失败和旧版挑战码等异常状态。

验收：数据验证、单元测试、浏览器测试和生产构建全部通过。

### 阶段九：试玩与 Gist 发布

- 标准构建输出 dist 目录。
- Gist 构建输出单个 game.html，并内联代码和游戏 JSON。
- Embedding 模型、原始缓存和相似度矩阵不进入网页。
- 本地验证单文件后上传 GitHub Gist，再通过 HTML 预览服务生成试玩链接。
- 分享链接使用 challenge 查询参数复现同一局。
- 根据朋友反馈重点修正无法理解的关系、难度和高频枢纽。

验收：朋友只需打开链接即可完成游戏和分享挑战，不需要安装或登录。

### 版本与更新规则

- datasetVersion 在图鉴数据变化时递增。
- graphVersion 在权重或关系边变化时递增。
- algorithmVersion 在随机挑战生成逻辑变化时递增。
- 单纯视觉修改不改变挑战码版本。
- 每次发布前保留固定测试种子的结果，防止无意改变旧挑战。
